# 🏗️ 运行机制总纲（简版）

> 插件架构的**概览**，供面板快速了解。
> 完整机制说明（通信规则 / 输出架构 / 交互流程）在 GitHub 仓库：
> **`docs/ARCHITECTURE.md`**。

## 一句话

**游戏适配插件，不是插件适配游戏**——插件是"游戏大脑总管家"，
游戏只做玩法逻辑，情感 / 图片 / 语音 / 帮助 / 路由全部由插件统一负责。

## 数据流

```
玩家输入 → parse_input 路由 → 游戏 handle_action 返回结构化结果
    → brain 统一编排（推送 / 情感 / 帮助 / 状态锚）
    → 猫娘在聊天窗回应

"帮助 / XX帮助 功能名" → core/help 三层寻址（游戏 → 分组 → 指令）
    → 版式块 → 官方 UI Kit 视觉 HTML → 宿主内置浏览器出图 → 推送到聊天窗
```

## 关键机制

| 机制 | 说明 |
|------|------|
| 自动发现 | 启动扫描 `games/`，新增游戏零登记 |
| 输出通道 | 游戏不推送，brain 统一 push_message + summary |
| 渲染桥接 | 帮助图/页面/卡片全由插件渲染，游戏只给数据（禁止自绘） |
| 帮助寻址 | 游戏 → 分组(可两级) → 指令，别名直达；指令可出专属图 |
| 状态锚 | 游戏进行中定期注入上下文，LLM 不脱节 |
| 命令专属 | 每游戏指令带前缀，跨游戏不串台 |
| 发图桥接 | 图库在 `games/neko_photo/data/`（不进 git）；空库有兜底图；频率由 LLM 决定、插件封顶 |

> 深入阅读完整版 `docs/ARCHITECTURE.md` 与帮助契约 `docs/help-config.md`。
